stages:
  data_ingestion:
    cmd: python src/data/data_ingestion.py
    deps:
      - src/data/data_ingestion.py
    params:
      - data_ingestion.test_size
    outs:
      - data/raw:
          cache: false

  data_preprocessing:
    cmd: python src/data/data_preprocessing.py
    deps:
      - data/raw/train.csv
      - data/raw/test.csv
      - src/data/data_preprocessing.py
    outs:
      - data/preprocessed:
          cache: false

  model_building:
    cmd: python src/model/model_building.py
    deps:
      - data/preprocessed/train_processed.csv
      - src/model/model_building.py
    params:
      - model_building.max_features
      - model_building.ngram_range
      - model_building.learning_rate
      - model_building.max_depth
      - model_building.n_estimators
    outs:
      - models/trained_model/lgbm_model.pkl:
          cache: false
      - models/vectorizer_model/tfidf_vectorizer.pkl:
          cache: false

  model_evaluation:
    cmd: python src/model/model_evaluation.py
    deps:
      - models/trained_model/lgbm_model.pkl
      - models/vectorizer_model/tfidf_vectorizer.pkl
      - data/preprocessed/train_processed.csv
      - data/preprocessed/test_processed.csv
      - src/model/model_evaluation.py
    outs:
      - data/evaluation

  model_registration:
    cmd: python src/model/register_model.py
    deps:
      - data/evaluation/experiment_info.json
      - src/model/register_model.py
